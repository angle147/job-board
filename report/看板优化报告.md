# 济南校招看板优化报告

日期：2026-08-30  
项目：`D:\hanako\招聘求职\01-招聘数据与看板\job-board`

## 1. 项目结构图

```text
Windows 任务计划程序（每日 09:00）
  └─ daily_update.py
      ├─ 山东省国资委 → data/jobs.js
      ├─ 央企公告 / 山东高速 / 山东港口 → data/jobs_qyzp|sdhsg|sdport.js
      ├─ 中国铁路人才招聘网 → data/jobs_railway.js（新增）
      ├─ 中国公共招聘网事业单位 → data/jobs_institutions.js（新增）
      ├─ 应届生 / 海投 → data/jobs_yingjiesheng|haitou.js
      ├─ 应届生校对 → enrich.py
      ├─ 51job（禁用，原因已写入日志）
      ├─ cleanup_expired_jobs.py
      ├─ 国考（周日）→ data/exams.js
      ├─ 微博招聘会（禁用，原因已写入日志）
      ├─ GitHub 同步 → GitHub Pages
      └─ 每日摘要 → 飞书（每日最多一次）

index.html
  ├─ 通过 <script> 加载各 data/*.js
  ├─ 各岗位数组合并为 ALL_JOBS
  └─ 浏览器端完成筛选、排序和表格/卡片渲染
```

岗位数据字段规范：`id`、`companyName`、`companyType`、`industry`、`recruitType`、`targetYears`、`location`、`positions`、`status`、`updateTime`、`deadline`、`applyLink`、`noticeLink`、`examInfo`、`companyScale`、`notes`。新增源均按此结构输出。

## 2. 已知异常修复

| 异常 | 根因 | 处理 | 验证 |
|---|---|---|---|
| GitHub 同步 `WinError 2` | 任务计划程序环境的 `PATH` 中没有 `git.exe`；仓库目录和 `origin` 均存在 | 新增 `_find_git()`，依次检查 PATH、Git for Windows 常用位置和本机 Codex runtime；每个 Git 子命令检查返回码 | `push --dry-run` 成功；完整日更实际 push 成功，提交 `470f032` |
| 国考超过 5 分钟 | 42 个部门逐页采集，没有单部门页数上限；请求与重试总耗时不可控 | 单部门默认限 2 页，调度显式传参；限页结果与既有 `exams.js` 按“部门+职位代码”合并，避免条数下降；请求间隔调整为 2 秒 | 完整交通部门限 1 页测试耗时 193.5 秒，退出码 0，仍为 3,884 条 |
| 51job 状态不明 | 依赖的 `D:\hanako\Auto-JobHunter-main`、采集脚本和 SQLite 数据库均已不存在；历史日志还记录多次 3 分钟超时 | 保持禁用，不尝试绕过反爬；调度日志显示具体禁用原因 | 完整日更明确记录两段禁用原因 |
| 微博状态不明 | `crawl4weibo` 需浏览器临时 Cookie；27 个关键词历史运行频繁超过 2 分钟 | 保持禁用，不绕过平台校验；调度日志显示具体禁用原因 | 完整日更明确记录禁用原因；历史日志可复核 |

## 3. 候选源探测表

| 源 | 探测入口 | 可达性 | 结构 / 反爬 | 判定与建议 |
|---|---|---|---|---|
| 国聘 | `https://www.iguopin.com/` | 200；robots 允许 | React SPA；公开 API 使用签名校验 | 需 JS/签名，不实现，不绕过校验 |
| 江苏国资委 | `https://gzw.jiangsu.gov.cn/` | 本机直连 15 秒超时 | 未取得页面结构 | 暂缓，后续换网络复测 |
| 浙江国资委 | `https://gzw.zj.gov.cn/` | 403 | 访问控制 | 不硬刚反爬 |
| 上海国资委 | `https://www.gzw.sh.gov.cn/` | 200 | 静态首页；招聘栏目需逐栏目适配 | 可作为后续独立适配项 |
| 北京国资委 | `https://gzw.beijing.gov.cn/` | 200 | 静态首页；招聘栏目需逐栏目适配 | 可作为后续独立适配项 |
| 智联招聘 | `https://www.zhaopin.com/robots.txt` | 200 | 岗位检索依赖动态接口，反爬较强 | 不实现 |
| 猎聘校招 | `https://campus.liepin.com/robots.txt` | 200 | 动态页面 | 暂缓 JS 方案 |
| 京东校园招聘 | `https://campus.jd.com/` | 200 | SPA，公开首页含验证码组件；岗位接口未直接开放 | 不实现，不绕过验证码 |
| 中远海运 / 招商局 / 顺丰 / 中国物流集团 | 各官网候选入口 | 本机 DNS、TLS 或连接失败 | 无法稳定取得结构 | 登记待后续，不据此认定站点永久不可用 |
| 山东港口 | 现有 `scrape_sdport.py` | 已运行 | 静态/轻接口 | 已有源，按要求跳过新增 |
| 中国铁路人才招聘网 | 公开列表与 `pageList` 接口 | 200 | 公开接口，轻反爬；请求间隔 2.1 秒 | 可爬，已实现 |
| 中国公共招聘网事业单位 | `http://job.mohrss.gov.cn/cjobs/institution/listInstitution` | 200 | 静态 HTML | 可爬，已实现交通物流关键词筛选 |

## 4. 新增源实现

| 源 | 脚本 | 轻量测试 | 完整日更状态 |
|---|---|---:|---|
| 中国铁路人才招聘网 | `scrape_railway.py` | 1 页 10 条，字段完整 | 2 页取得 40 条；均已过明确截止日期，清理后 0 条（预期行为） |
| 中国公共招聘网事业单位 | `scrape_institutions.py` | 2 条 | 2 条保留并进入看板 |

两个新源均已接入 `daily_update.py`、`index.html`、摘要统计和过期清理。铁路接口当前没有未过期公告，但采集链路与字段映射已验证；没有为凑数保留失效岗位。

## 5. 回归与验收结果

- Python 编译与 AST 解析通过；新增源均可独立运行。
- 国考完整验证：193.5 秒、3,884 条，未因限页刷新而减少。
- 快速完整日更：9 成功、0 失败；GitHub Pages 实际同步成功。
- 完整日更后的有效岗位数：山东省国资委 17、应届生 320、海投 90、央企公告 34、山东高速 4、山东港口 1、事业单位新源 2、铁路新源 0（40 条均已过期）。
- 原数据字段没有被重命名；新增源使用完整 16 字段结构。既有部分旧源本身缺少 `companyScale`/`notes`，本次未扩大范围改写旧源。
- 日更清理共移除 128 条已过期岗位，因此“有效条数”相较清理前下降；这是既有过期规则的正常结果，不是采集回归。原采集阶段各既有源运行成功。
- `feishu_config.json` 无差异；未读取或输出其中凭据。
- 飞书推送因当日已成功推送过而按既有规则跳过，未重复发送。

## 6. 回滚与未完成项

- 代码修改前提交：`e161c34`。
- 本次数据同步提交：`470f032`；需要回滚线上数据时可对该提交执行常规 Git revert。
- 上海、北京国资委可达，但栏目结构适配尚未实现；江苏需换网络复测；浙江返回 403。
- 国聘、智联、猎聘、京东等动态/校验型来源未绕过反爬，按工程纪律保留为后续评估项。

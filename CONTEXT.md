# 校招岗位看板

济南及周边地区交通/物流/仓储类校招岗位的自动化采集、清洗与展示系统。数据每日自动更新，前端托管于 GitHub Pages。

## 术语

**岗位（Job）**
一条招聘信息的最小单位。包含标题、单位、城市、截止日期（deadline）、行业标签、详情链接。以 JS 数组形式存储，前端通过 `<script>` 标签加载。
_Avoid_: 职位、工作、条目、记录

**数据源（Source）**
岗位数据的来源渠道。每个源有独立的采集脚本和输出文件。当前系统有五个源：国资委、应届生求职网、海投网、51job、手动维护。
_Avoid_: 渠道、爬虫源

**采集（Scrape）**
从数据源拉取最新岗位的过程。每个采集脚本独立运行，输出为 `data/jobs_<源>.js`。采集采用追加+去重策略——新岗位追加，已存在的跳过。
_Avoid_: 抓取、爬取、拉取

**校对（Enrich）**
对已采集的岗位补充行业标签、城市等信息。当前仅应届生求职网数据需要校对。校对过程通过分析岗位标题自动推断行业分类。
_Avoid_: 补充、打标签

**截止日期（Deadline）**
岗位的申请截止日期。过期岗位在清理阶段被移除。特殊值「招满为止」表示无固定截止日期，需从备注或更新时间推断有效期。
_Avoid_: 到期日、过期日、DDL

**过期清理（Cleanup）**
根据 deadline 字段自动移除已失效岗位的过程。策略：含明确日期的过期为删；「招满为止」类从 notes/updateTime 提取日期，超过 60 天则删。每个源独立清理。
_Avoid_: 删除、归档、下线

**流水线（Pipeline）**
每日自动运行的端到端流程：各源采集 → 校对 → 51job 导出 → 过期清理 → 国考（仅周日）→ 微博招聘会 → Git 同步 → 推送通知。由 `daily_update.py` 编排。
_Avoid_: 自动化任务、定时任务、工作流

**招聘会（Event）**
线下招聘会信息。来自两个渠道：高校就业平台（静态 JSON）和微博校园招聘会（爬虫采集）。与岗位数据分开存储和展示。
_Avoid_: 宣讲会、活动、校招会

**国考（Exam）**
国家公务员考试中交通相关职位。仅在周日从高校就业平台采集，每周更新一次，频率低于日更岗位。
_Avoid_: 公考、公务员考试

**通知推送（Notification）**
流水线完成后，将当日更新摘要推送至微信和飞书的动作。推送内容包含各源岗位数、招聘会场次、过期清理统计。
_Avoid_: 消息推送、提醒、微信通知

**锁文件（Lock）**
`.daily_update.lock`，流水线启动时写入，完成后删除。30 分钟内再次触发则跳过，防止重复运行。过期的锁文件（>30 分钟）会被忽略。
_Avoid_: 互斥锁、信号量、pid 文件

## 数据文件

| 文件 | 源 | 类型 |
|------|----|------|
| `jobs.js` | 山东省国资委 | 岗位 |
| `jobs_yingjiesheng.js` | 应届生求职网 | 岗位 |
| `jobs_haitou.js` | 海投网 | 岗位 |
| `jobs_51job.js` | 51job | 岗位 |
| `jobs_manual.js` | 手动维护 | 岗位 |
| `university_events.js` | 高校就业平台 | 招聘会 |
| `weibo_events.js` | 微博 | 招聘会 |
| `exams.js` | 高校就业平台 | 国考 |

## 关系

- **采集 → 校对**：应届生求职网的采集结果先经过校对补充标签，再进入数据文件
- **所有源 → 过期清理**：清理阶段遍历所有源的数据文件，按 deadline 独立清理
- **流水线 → GitHub Pages**：每次流水线完成后 git push，前端自动部署
- **流水线 → 通知推送**：推送内容是流水线产出的 `.daily_summary.txt`，不依赖外部 cron 通知任务
